BEGIN
  EXECUTE IMMEDIATE 'DROP TABLE USERTABLE';
EXCEPTION
  WHEN OTHERS THEN
    IF SQLCODE != -942 THEN
      RAISE;
    END IF;
END;

CREATE TABLE USERTABLE (
    IotBench_KEY INT NOT NULL,
    FIELD1 NUMBER(20),
    FIELD2 NUMBER(10, 2),
    FIELD3 NUMBER(10, 2),
    CONSTRAINT IotBench_KEY_PK PRIMARY KEY (IotBench_KEY)
);
