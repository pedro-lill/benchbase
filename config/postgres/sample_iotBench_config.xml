<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>postgres</type>
    <driver>org.postgresql.Driver</driver>
    <url>jdbc:postgresql://localhost:5432/iotbench?rewriteBatchedStatements=true&amp;ApplicationName=iotbench&amp;sslMode=disable</url>
    <username>postgres</username>
    <password>password</password>
    <reconnectOnConnectionFailure>true</reconnectOnConnectionFailure>
    <isolation>TRANSACTION_SERIALIZABLE</isolation>
    <batchsize>1000</batchsize>

    <!-- Scalefactor in iotbench is *1000 the number of rows in the USERTABLE-->
    <scalefactor>1.0</scalefactor>

    <!-- Optional: Override the field size for each column in USERTABLE -->
    <!-- <fieldSize>8</fieldSize> -->

    <!-- Optional: Override the zipfian constant to modify the skew -->
    <!-- <skewFactor>0.99</skewFactor> -->

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>60</time>
            <rate>10000</rate>
            <weights>50,5,15,10,10,10</weights>
        </work>
    </works>

    <!-- iotbench Procedures declaration -->
    <transactiontypes>
        <transactiontype>
            <name>SensorReading</name>
        </transactiontype>
        <transactiontype>
            <name>DeviceControl</name>
        </transactiontype>
        <transactiontype>
            <name>AutomationTrigger</name>
        </transactiontype>
        <transactiontype>
            <name>GetDeviceStatus</name>
        </transactiontype>
        <transactiontype>
            <name>GetSensorHistory</name>
        </transactiontype>
        <transactiontype>
            <name>GetRoomOverview</name>
        </transactiontype>
    </transactiontypes>
</parameters>
